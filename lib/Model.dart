class Invoice {
  dynamic? productName,productDiscount,productQty,productPrice,productAmount;

  Invoice(this.productName, this.productDiscount, this.productQty,
      this.productPrice, this.productAmount);
}
